name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  merge_group:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  verify:
    name: Verify
    if: github.event_name != 'pull_request' || !github.event.pull_request.draft
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Validate GitHub Actions workflows
        run: |
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.7
          "$(go env GOPATH)/bin/actionlint"

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check dependency integrity
        run: pnpm check:dependencies

      - name: Check formatting
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Check circular dependencies
        run: pnpm lint:circular

      - name: Test, typecheck, and build
        env:
          NODE_OPTIONS: --max-old-space-size=6144
          NX_MAX_PARALLEL: 2
        run: pnpm exec nx run-many -t test typecheck build --all --parallel=2

  runner-autoscaler:
    name: runner-autoscaler
    # Plain Go module, not wired into the Nx graph above (revisit later) --
    # go build/vet/test need no network beyond the module proxy, so this
    # runs on ubuntu-latest same as verify. Building/publishing the actual
    # container images is a separate, self-hosted workflow (needs the
    # internal registry's LAN).
    if: github.event_name != 'pull_request' || !github.event.pull_request.draft
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: runner-autoscaler

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version-file: runner-autoscaler/go.mod

      - run: go build ./...
      - run: go vet ./...
      - run: go test ./...
