/**
 * Future home for pure lifecycle reducer and port implementations.
 *
 * This library deliberately exports no behavior in #1021: the schemas in
 * @agent-lcars/dispatch-contracts define the cross-service boundary first.
 */
export {};
