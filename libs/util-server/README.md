# util-server

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-server` to build the library.

## Running unit tests

Run `nx test util-server` to execute the unit tests via [Jest](https://jestjs.io).
