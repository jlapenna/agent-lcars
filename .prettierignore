/dist
/coverage

# Committed esbuild output (see .gitignore's negation of the blanket `dist`
# ignore, and .github/actions/project.json's `build` target): a generated
# bundle, not hand-authored source, so it has no business being
# Prettier-formatted -- and doing so would fight esbuild's own output
# formatting on every rebuild.
.github/actions/dispatch-broker/dist

# Same generated-bundle exemption as dispatch-broker/dist above, produced
# by apps/dispatch-broker's `build-canary` target instead of `build`.
.github/actions/run-dispatch-canary/dist

# Same generated-bundle exemption as dispatch-broker/dist above, produced
# by apps/rerun-infra-killed-runs's `build` target (agent-lcars#1183: moved
# out of apps/dispatch-broker so the published action no longer depends on
# that app's fate).
.github/actions/rerun-infra-killed-runs/dist

# Same generated-bundle exemption as the sibling operational actions above,
# produced by apps/dispatch-broker's `build-deliverable-watchdog` target.
.github/actions/deliverable-watchdog/dist

# Same generated-bundle exemption as the sibling operational actions above,
# produced by apps/dispatch-broker's `build-webhook-ingress-canary` target.
.github/actions/run-webhook-ingress-canary/dist
/.nx/cache
/.nx/workspace-data

# Generated by Next.js during install and build.
apps/console/next-env.d.ts

# pnpm owns the canonical lockfile representation.
pnpm-lock.yaml

# Generated and local agent state.
.agent/
.gemini/
.jules/
.worktrees/
