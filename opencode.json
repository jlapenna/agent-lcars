{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "external_directory": "allow",
    "doom_loop": "allow"
  },
  "compaction": {
    "reserved": 3000,
    "prune": true
  },
  "provider": {
    "homelab": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Homelab LiteLLM (spark)",
      "options": {
        "baseURL": "https://llm.lan.jlapenna.net/v1",
        "apiKey": "{env:OPENCODE_LLM_API_KEY}"
      },
      "models": {
        "default": {
          "name": "Spark Default (DeepSeek-V4-Flash, pinned production route - no swap dependency). context 60000: see docs/opencode-context-limit.md - it is neither the backend's 262144 nor a guess, and changing it changes how often the agent loses its working memory.",
          "limit": {
            "context": 60000,
            "output": 2048
          }
        }
      }
    }
  }
}
