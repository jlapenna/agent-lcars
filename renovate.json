{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>jlapenna/agent-lcars//renovate-preset",
    "monorepo:nx",
    "monorepo:nextjs",
    "monorepo:react"
  ],
  "packageRules": [
    {
      "description": "pnpm majors change install/lockfile semantics for the whole workspace at once (packageManager pins the exact version, currently 11.x); hold them for a deliberate toolchain upgrade rather than a bot PR",
      "enabled": false,
      "matchPackageNames": ["pnpm"],
      "matchUpdateTypes": ["major"]
    },
    {
      "description": "Resolve github.com/google/uuid through the public Go module proxy. This avoids the hosted no-result lookup while keeping Renovate checks and future updates enabled.",
      "matchManagers": ["gomod"],
      "matchPackageNames": ["github.com/google/uuid"],
      "registryUrls": ["https://proxy.golang.org"]
    }
  ],
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["bot:renovate", "type:security"]
  }
}
